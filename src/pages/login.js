//external imports
import React from "react";

//internal imports
import Login from "@/components/authentication/Login";

const index = () => {
  return (
    <div>
      <Login></Login>
    </div>
  );
};

export default index;
